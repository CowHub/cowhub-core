machine:
  environment:
    # Setting the tag for Docker-hub
    TAG: $CIRCLE_BRANCH-$CIRCLE_SHA1
    DOCKER_IMAGE: cowhub/cowhub-core:$CIRCLE_BUILD_NUM-$CIRCLE_BRANCH-$CIRCLE_SHA1
  services:
    - docker

dependencies:
  override:
    - docker info

test:
  override:
    - echo $DOCKER_IMAGE
    - docker build --rm=false -f Dockerfile.ami .
